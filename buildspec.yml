version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies...
      - apt-get update -y
  build:
    commands:
      - echo Building the application...
      - make build
  post_build:
    commands:
      - echo Build completed successfully!
artifacts:
  files:
    - "**/*"
  discard-paths: yes
